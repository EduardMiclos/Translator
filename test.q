# program de test

function max(x:int, y:int):int
    var a:int;

    if(x<a)
        return y;
    else
        return x;
    end
end

var i:int;
i=0;    # iterator
while(i<10)
    puti(max(i,5));
    i=i+max;
    end

puts("PI=");
putr(3.14159);